import React, {useEffect, useState} from 'react';const Request = ({page}) => {    const [active, setActive] = useState(false)    const [counts, setCounts] = useState(0)    function send() {        const options = {            method: "GET",            mode: "no-cors"        }        fetch("http://" + page, options)            .then(res => res.text())            .then(html => {                const parser = new DOMParser();                const doc = parser.parseFromString(html, 'text/html');                setActive(true)            }).catch(e => {            setActive(false)        })    }    useEffect(() => {        send()    }, [])    if (active) {        setTimeout(() => {            send()            setCounts(counts + 1)        }, 10)    }    setTimeout(()=>{        send()    },3000)    return (        <div>            <div className='items'>                <div className='item'>{page}</div>                <div className='item' style={{color: active ? 'green' : 'red'}}>{active ? 'Online' : 'Offline'}</div>                <div className='item'>{counts}</div>            </div>        </div>    );};export default Request;